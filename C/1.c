#include <stdio.h>
#include <math.h>


int main()
{
    int fib1 = 1;
    int fib2 = 2;

    for (int )

    int result = SumDivisibleBy(n1)+SumDivisibleBy(n2)-SumDivisibleBy(n3);
    printf("Result: %i", result);
    return 0;
}

